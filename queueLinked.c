#include<stdio.h>
#include<stdlib.h>
#include<time.h>

//queue linkedlist

struct node{
    int val;
    struct node* next;
};

typedef struct node* nodeAddress;

nodeAddress enqueue(int val){
    nodeAddress temp = 
}